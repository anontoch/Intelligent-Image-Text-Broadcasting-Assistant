# Intelligent-Image-Text-Broadcasting-Assistant
An intelligent image text broadcasting assistant built with Python. The project reads a local image, extracts text using Baidu OCR, saves the recognized result, converts the text into speech using Baidu TTS, and generates an MP3 audio file for playback.
